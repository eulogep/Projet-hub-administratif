import { notFound } from "next/navigation"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { getActiveWorkspace } from "@/lib/auth/get-active-workspace"; import { createCrousLogAction } from "@/modules/crous-hours/actions/crous-hours.actions"; import { CrousLogForm } from "@/modules/crous-hours/components/crous-log-form"; import { getCrousPeriod } from "@/modules/crous-hours/services/crous-hours.service";
export default async function NewCrousLogPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const workspace = await getActiveWorkspace(); const period = await getCrousPeriod(workspace.id, id); if (!period) notFound(); return <main id="main-content" tabIndex={-1} className="mx-auto w-full max-w-3xl space-y-6 px-4 py-7 outline-none sm:px-6 sm:py-9"><h1 className="text-2xl font-semibold">Nouvelle intervention</h1><Card><CardHeader><CardTitle>{period.label}</CardTitle></CardHeader><CardContent><CrousLogForm action={createCrousLogAction} periodId={id} cancelHref={`/crous-hours/${id}`} /></CardContent></Card></main>; }
