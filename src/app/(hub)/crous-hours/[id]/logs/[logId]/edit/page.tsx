import { notFound } from "next/navigation"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { getActiveWorkspace } from "@/lib/auth/get-active-workspace"; import { updateCrousLogAction } from "@/modules/crous-hours/actions/crous-hours.actions"; import { CrousLogForm } from "@/modules/crous-hours/components/crous-log-form"; import { getCrousLog, getCrousPeriod } from "@/modules/crous-hours/services/crous-hours.service";
export default async function EditCrousLogPage({ params }: { params: Promise<{ id: string; logId: string }> }) { const { id, logId } = await params; const workspace = await getActiveWorkspace(); const [period, log] = await Promise.all([getCrousPeriod(workspace.id, id), getCrousLog(workspace.id, logId)]); if (!period || !log || log.period_id !== id) notFound(); return <main id="main-content" tabIndex={-1} className="mx-auto w-full max-w-3xl space-y-6 px-4 py-7 outline-none sm:px-6 sm:py-9"><h1 className="text-2xl font-semibold">Modifier l’intervention</h1><Card><CardHeader><CardTitle>{period.label}</CardTitle></CardHeader><CardContent><CrousLogForm action={updateCrousLogAction.bind(null, logId)} log={{ period_id: id, starts_at: log.starts_at, ends_at: log.ends_at, credited_minutes: log.credited_minutes, adjustment_reason: log.adjustment_reason, notes: log.notes }} periodId={id} cancelHref={`/crous-hours/${id}`} /></CardContent></Card></main>; }
