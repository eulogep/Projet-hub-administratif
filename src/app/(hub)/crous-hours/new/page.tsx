import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { getActiveWorkspace } from "@/lib/auth/get-active-workspace"; import { createCrousPeriodAction } from "@/modules/crous-hours/actions/crous-hours.actions"; import { CrousPeriodForm } from "@/modules/crous-hours/components/crous-period-form"; import { listActiveCrousMissions } from "@/modules/crous-hours/services/crous-hours.service";
export default async function NewCrousPeriodPage() { const workspace = await getActiveWorkspace(); const missions = await listActiveCrousMissions(workspace.id); return <main id="main-content" tabIndex={-1} className="mx-auto w-full max-w-3xl space-y-6 px-4 py-7 outline-none sm:px-6 sm:py-9"><h1 className="text-2xl font-semibold">Nouvelle période CROUS</h1><Card><CardHeader><CardTitle>Objectif de la période</CardTitle></CardHeader><CardContent>{missions.length ? <CrousPeriodForm action={createCrousPeriodAction} missions={missions} /> : <p role="alert">Créez d’abord une mission liée à une organisation de type CROUS.</p>}</CardContent></Card></main>; }
